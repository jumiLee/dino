<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="mgrVariable"> 

	<select id="selectVariableValue" parameterType="first.common.util.ParamVO" resultType="INTEGER"> 
		select var_value 
		  from mgr_variable 
		 where var_category = #{inParam01,mode=IN,jdbcType=INTEGER}	<!-- var_category-->
		   and var_id = #{inStrParam01,mode=IN,jdbcType=VARCHAR}	<!-- var_id-->
	</select>
</mapper>